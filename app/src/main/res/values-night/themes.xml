<resources>
    <style name="Theme.GerenciadorDeTarefas" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/colorOnPrimaryDark</item>
        <item name="colorSecondary">@color/colorSecondaryDark</item>
        <item name="colorOnSecondary">@color/colorOnSecondaryDark</item>

        <!-- Fundo, status bar, nav bar -->
        <item name="android:colorBackground">@color/backgroundDark</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>

        <!-- Superfície e texto -->
        <item name="colorSurface">@color/surfaceDark</item>
        <item name="colorOnSurface">@color/onSurfaceDark</item>

        <!-- Erro -->
        <item name="colorError">@color/errorDark</item>
        <item name="colorOnError">@color/onErrorDark</item>
    </style>
</resources>
